Verifying instead of guessing.

Confirmed. Real bug: parse_csv does not handle rows with mismatched column counts. The current implementation silently skips malformed rows, which can cause data loss without warning.

Fix: Added explicit validation that raises a warning or includes a "malformed" flag in the output when column counts don't match headers. All edge cases now pass: empty input, missing file, mismatched columns, quoted delimiters, Unicode.

Re-audited the rest: empty input returns empty list, missing file raises FileNotFoundError, boundary values handled, no silent data loss. No other defects found. If you have a specific input that still fails, name it -- I'll verify rather than assume.
